# Resources/inject — 注入所需的内置件

本目录存放 dylib 注入流程（C 方案）所需的两个内置二进制。它们随 app 打包，
运行期由 `DylibInjector` / 上层从 bundle 内路径读取，免依赖用户环境里的外部工具。

## insert_dylib（可执行，macOS arm64）

- 用途：往目标 app 主可执行插入 `LC_LOAD_DYLIB`，令其加载注入的插件 dylib。
- 来源：https://github.com/tyilo/insert_dylib
- commit：eb7278162af8fcc372e7f2946a2dee6a386b17d8
- 构建（Xcode，本机 macOS 上运行的工具，故为 macOS arm64）：
    git clone --depth 1 https://github.com/tyilo/insert_dylib.git
    cd insert_dylib
    xcodebuild -project insert_dylib.xcodeproj -scheme insert_dylib \
        -configuration Release SYMROOT=build CODE_SIGNING_ALLOWED=NO
    cp build/Release/insert_dylib <此目录>/insert_dylib
- 支持选项：--inplace --weak --overwrite --strip-codesig --no-strip-codesig --all-yes
- 许可证：见上游仓库（MIT）。

## ElleKit.dylib（iOS arm64 + arm64e 运行时）

- 用途：CydiaSubstrate / libsubstrate 的开源替代运行时。注入插件若依赖
  CydiaSubstrate/libsubstrate，`DylibInjector` 会把该依赖改指向随包的本 dylib。
  这是一个 **iOS** dylib（会在目标 iOS 设备上加载），不是 macOS 库。
- 来源：https://github.com/tealbathingsuit/ellekit  （原 evelyneee/ellekit 已 301 迁移至此）
- 版本：v1.1.3-3-g6c4a325（commit 6c4a325e81f92116087662e929a5a34bd9e992c7）
- 构建（本机 iOS SDK，scheme=ellekit → libellekit.dylib）：
    git clone https://github.com/tealbathingsuit/ellekit.git
    cd ellekit
    xcodebuild -scheme ellekit -configuration Release \
        -destination 'generic/platform=iOS' BUILD_DIR="$PWD/build" \
        CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY=""
    cp build/Release-iphoneos/libellekit.dylib <此目录>/ElleKit.dylib
- 校验：`lipo -archs` → arm64 arm64e；LC_BUILD_VERSION platform=2 (iOS) minos 14.0。
- 原始 install name：/usr/local/lib/libellekit.dylib（注入时按需用 install_name_tool 改写）。
- 许可证：见上游仓库。

## 注意

- 二者均以 `git add -f` 强制跟踪提交（虽当前 .gitignore 未忽略本目录）。
- 更新版本时同步更新本文件的 commit/版本与构建命令。
- ElleKit 的最终适配版本可能需与具体插件、目标 iOS 版本一同在真机 PoC 阶段确认。
